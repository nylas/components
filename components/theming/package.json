{
  "name": "@nylas/components-theming",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'Nothing to build'",
    "dev": "yarn run build",
    "tscheck": "echo 'Nothing to check'",
    "start": "echo 'Nothing to start'"
  }
}
